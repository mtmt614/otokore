<div class="container col-12">
  <h4 class="text-center bg-light"><span style="background-color: gainsboro">新規投稿</span></h4>
  <div class="row g-3 d-flex justify-content-center">
    <%= form_with model: @post do |f| %>

      <div class="row mb-3">
        <%= f.label "曲名", class: "col-md-3 col-form-label" %>
        <%= f.text_field :title, class: "col-md-5 form-control item_name" %>
      </div>
      
      <div class="row mb-3">
        <%= f.label "アーティスト", class: "col-md-3 col-form-label" %>
        <%= f.text_field :artist, class: "col-md-5 form-control item_name" %>
      </div>
      
      <div class="row mb-3">
        <%= f.label "ジャンル", class: "col-md-3 form-select" %>
        <%= f.select :genre_id, options_from_collection_for_select(Genre.all, :id, :genrename) %>
      </div>

      <div class="row mb-3">
        <%= f.label "ひとこと", class: "col-md-3 col-form-label" %>
        <%= f.text_area :content, class: "col-md-5 form-control item_description" %>
      </div>

      <div class="row mb-3 justify-content-center">
        <%= f.submit "新規投稿", class: "col-md-2 btn btn-outline-success btn-lg" %>
      </div>
    </div>
    <% end %>
  </div>