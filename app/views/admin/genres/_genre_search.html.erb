<tbody>
    <% genres.each do |genre| %>
    <tr>
      <td>
        <%= link_to genre.genrename, posts_path(genre_id: genre.id), class: "text-secondary" %>
      </td>
    </tr>
    <% end %>
</tbody>